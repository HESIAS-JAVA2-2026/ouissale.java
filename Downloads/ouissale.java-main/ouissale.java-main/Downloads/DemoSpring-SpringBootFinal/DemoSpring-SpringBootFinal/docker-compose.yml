version: '3.8'

services:
  db:
    image: postgres:15
    container_name: postgres-demo
    restart: always
    environment:
      POSTGRES_DB: demo
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
